// reducers/reducer.js

const initialState = {
	// todo : add	
};
  
  const rootReducer = (state = initialState, action) => {
    switch(action){
      // todo : case #TYPE# : return {...state, #?#}
    }
  };
  
  export default rootReducer;